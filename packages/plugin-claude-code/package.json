{
  "name": "@agent-iris/claude-code-channel",
  "version": "0.1.1",
  "description": "Claude Code channel plugin for agent-iris — connects iris server to Claude Code CLI via WebSocket",
  "main": "./dist/index.js",
  "bin": {
    "claude-code-channel": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "NODE_ENV=production bun build src/index.ts --outdir dist --target node --sourcemap=external --define 'process.env.NODE_ENV=\"production\"' && chmod +x dist/index.js && tsc --noEmit",
    "prepublishOnly": "bun run build",
    "start": "node dist/index.js",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-iris/protocol": "workspace:*",
    "pino": "^9.3.2",
    "pino-pretty": "^13.1.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13"
  },
  "keywords": [
    "claude",
    "claude-code",
    "iris",
    "agent",
    "bot",
    "gateway"
  ],
  "license": "MIT"
}
